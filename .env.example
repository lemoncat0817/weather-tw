# 複製這個檔案為 .env（已被 .gitignore 排除，不會進版控）並填入實際值。
#
# CWA API 金鑰只存在於伺服器端（server/utils/cwa.ts 透過 runtimeConfig 讀取），
# 前端 bundle 完全看不到，也不會出現在任何 API 回應內容裡。
# 到 https://opendata.cwa.gov.tw 註冊會員即可取得金鑰。
NUXT_CWA_API_KEY=

# 地圖底圖樣式網址。預設用 OpenFreeMap 的免金鑰深色 vector style，
# 無需註冊、無流量限制（基於 OpenStreetMap 資料，見 https://openfreemap.org）。
NUXT_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/dark
