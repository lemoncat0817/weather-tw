# 複製這個檔案為 .env.local（已被 .gitignore 的 *.local 規則排除）即可覆寫下面的值。
# Worker 網址不含機密資訊，可以安全提交進 git；真正的 CWA API 金鑰只存在於
# Cloudflare Worker 的 secret 裡（見 worker/README.md），不會出現在前端任何地方。

VITE_API_BASE_URL=https://vue3-weather-proxy.YOUR-SUBDOMAIN.workers.dev
