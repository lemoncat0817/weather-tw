{
  "name": "vue3-weather-proxy",
  "private": true,
  "version": "0.0.0",
  "description": "把 CWA_API_KEY 藏在伺服器端的 Cloudflare Worker，代理前端對中央氣象署開放資料平台的所有請求。",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "~5.4.0",
    "wrangler": "^4"
  }
}
